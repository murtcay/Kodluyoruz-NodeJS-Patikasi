## Trianlge Area Calculator

Write a program that calculates the area of the triangle whose three side lengths you get from the user.

**Formula**:

```
perimeter = (a+b+c) = 2 *u
area * area = (u * (u - a) * (u - b) * (u - c))
```
